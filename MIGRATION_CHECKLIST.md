# ✅ Чек-лист переезда ChildDev на новый компьютер

## 📦 Что на флешке:
- Все файлы проекта из `childdev-cl/`
- Актуальная версия: коммит `3b3b2c5`
- Дата бэкапа: 2025-11-06

---

## 🖥️ На новом компьютере

### Шаг 1: Установка необходимого ПО ⏱️ ~15 минут

```bash
# Проверьте, что установлено:
node --version    # Должно быть >= 18.0.0
npm --version     # Обычно идёт с Node.js
git --version     # Для работы с репозиторием
```

**Если чего-то не хватает:**
- Node.js v18+: https://nodejs.org/
- Git: https://git-scm.com/
- Docker Desktop (опционально): https://www.docker.com/products/docker-desktop/

### Шаг 2: Копирование проекта ⏱️ ~5 минут

**Вариант A: С флешки**
```bash
# Скопируйте папку с флешки в рабочую директорию
cp -r /Volumes/YOUR_USB/childdev-cl ~/Projects/
cd ~/Projects/childdev-cl
```

**Вариант B: Из GitHub (рекомендуется)**
```bash
# Если есть доступ к интернету
git clone git@github.com:zarudesu/children-develop.git childdev-cl
cd childdev-cl
```

### Шаг 3: Настройка окружения ⏱️ ~10 минут

```bash
# Автоматическая настройка
./scripts/dev-setup.sh
```

**Что делает этот скрипт:**
- ✅ Устанавливает зависимости веб-сервиса
- ✅ Устанавливает зависимости PDF-сервиса
- ✅ Скачивает Playwright браузеры
- ✅ Создаёт .env файлы
- ✅ Создаёт необходимые директории

### Шаг 4: Первый запуск ⏱️ ~2 минуты

```bash
# Запустить локальную разработку
./scripts/run-local.sh
```

**Должны запуститься два процесса:**
- 🌐 Веб-сервис на http://localhost:3002
- 📄 PDF-сервис на http://localhost:3001

### Шаг 5: Проверка работоспособности ⏱️ ~3 минуты

```bash
# В новом терминале проверьте health endpoints
curl http://localhost:3001/health  # PDF service
curl http://localhost:3002/api/health  # Web service

# Или используйте скрипт
./scripts/check-health.sh
```

**Проверка в браузере:**
1. Откройте http://localhost:3002
2. Должна загрузиться главная страница с 5 генераторами
3. Попробуйте создать филворд:
   - Филворды → Заполните форму → Создать PDF
   - Должен скачаться PDF файл

---

## 🔧 Если что-то пошло не так

### Проблема: "command not found: node"
**Решение:** Установите Node.js v18+ с https://nodejs.org/

### Проблема: "Playwright browsers not found"
```bash
cd services/pdf
npx playwright install chromium
```

### Проблема: "Port 3001/3002 already in use"
```bash
# Найдите процесс
lsof -i :3001
lsof -i :3002

# Убейте процесс
kill -9 <PID>

# Или остановите все сервисы
./scripts/stop-local.sh
```

### Проблема: TypeScript ошибки
```bash
# Переустановите зависимости
cd services/web
rm -rf node_modules package-lock.json
npm install

cd ../pdf
rm -rf node_modules package-lock.json
npm install
```

### Проблема: Ошибка генерации PDF
```bash
# Убедитесь, что PDF-сервис запущен
curl http://localhost:3001/health

# Проверьте логи
tail -f logs/pdf-service.log
```

---

## 📱 Настройка Git (опционально)

```bash
# Настройте Git пользователя
git config --global user.name "Ваше Имя"
git config --global user.email "your.email@example.com"

# Проверьте статус
git status
git log --oneline -5
```

---

## 🚀 Production (когда понадобится)

**SSH доступ настроен на старом компьютере.**

Если нужен доступ к production серверу:
1. Скопируйте SSH ключ: `~/.ssh/childdev_vps`
2. Скопируйте SSH конфиг из `~/.ssh/config` (секция `Host children-vps`)
3. Подключайтесь: `ssh children-vps`

**Production URL:** http://children.hhivp.com:3002

---

## ✅ Финальный чек-лист

После настройки убедитесь:

- [ ] Node.js v18+ установлен (`node --version`)
- [ ] npm установлен (`npm --version`)
- [ ] Git установлен (`git --version`)
- [ ] Проект скопирован в `~/Projects/childdev-cl`
- [ ] `./scripts/dev-setup.sh` выполнен без ошибок
- [ ] `./scripts/run-local.sh` запускается
- [ ] http://localhost:3002 открывается
- [ ] http://localhost:3001/health возвращает `{"status":"healthy"}`
- [ ] Генерация филворда работает (попробуйте создать)
- [ ] Git статус показывает `nothing to commit, working tree clean`

---

## 📚 Полезные документы

После успешной настройки изучите:
- **QUICK_START.md** - быстрый старт
- **docs/DEV_SETUP_CHECKLIST.md** - подробный гайд
- **CLAUDE.md** - правила разработки
- **docs/technical/ARCHITECTURE.md** - архитектура

---

## 🎉 Готово!

Если все пункты выполнены — можно работать!

**Типичный рабочий процесс:**
```bash
cd ~/Projects/childdev-cl
./scripts/run-local.sh       # Запустить
# Разработка...
./scripts/stop-local.sh      # Остановить
```

**Коммит изменений:**
```bash
git add .
git commit -m "✨ Описание изменений"
git push origin main
```

---

**Время на полную настройку:** ~35 минут
**Последняя актуализация:** 2025-11-06
**Актуальный коммит:** `3b3b2c5`
